# คู่มือการ Deploy เว็บไซต์ pamoontoy.site

## ✅ สถานะการติดตั้ง

- ✅ **ติดตั้ง Node.js และ dependencies สำเร็จ**
- ✅ **Build production files สำเร็จ**
- ✅ **ติดตั้งและตั้งค่า Nginx สำเร็จ**
- ✅ **ติดตั้ง Certbot สำเร็จ**
- ⏳ **รอการตั้งค่า DNS**

---

## 📋 ข้อมูลสำคัญ

- **โดเมน:** pamoontoy.site
- **IP Address:** 72.60.237.7
- **Web Server:** Nginx
- **ไฟล์เว็บไซต์:** /var/pamoontoyv2/dist

---

## 🔧 ขั้นตอนที่ต้องทำต่อ

### 1. ตั้งค่า DNS Records

ไปที่ผู้ให้บริการโดเมนของคุณ (เช่น Namecheap, GoDaddy, Cloudflare) และเพิ่ม DNS records ดังนี้:

#### Record ที่ 1 - Root Domain
```
Type: A
Name: @ (หรือเว้นว่าง)
Value: 72.60.237.7
TTL: Auto หรือ 3600
```

#### Record ที่ 2 - WWW Subdomain
```
Type: A
Name: www
Value: 72.60.237.7
TTL: Auto หรือ 3600
```

**หมายเหตุ:** DNS อาจใช้เวลา 5-48 ชั่วโมงในการแพร่กระจาย แต่มักจะเสร็จภายใน 15-30 นาที

---

### 2. ตรวจสอบว่า DNS ตั้งค่าเสร็จแล้วหรือยัง

รันคำสั่งนี้เพื่อตรวจสอบ:

```bash
nslookup pamoontoy.site
```

หรือ

```bash
dig pamoontoy.site
```

ถ้าเห็น IP address **72.60.237.7** แสดงว่า DNS ตั้งค่าเสร็จแล้ว

---

### 3. ติดตั้ง SSL Certificate (HTTPS)

หลังจาก DNS ตั้งค่าเสร็จแล้ว รันคำสั่งนี้:

```bash
cd /var/pamoontoyv2
./setup-ssl.sh
```

สคริปต์นี้จะ:
- ตรวจสอบว่า DNS ตั้งค่าถูกต้องหรือไม่
- ขอ SSL certificate ฟรีจาก Let's Encrypt
- ตั้งค่า Nginx ให้ใช้ HTTPS อัตโนมัติ
- ตั้งค่าให้ redirect HTTP → HTTPS

---

## 🌐 URL ที่ใช้งาน

### ขณะนี้ (ยังไม่มี SSL):
- http://72.60.237.7 ✅ ใช้งานได้แล้ว
- http://pamoontoy.site ⏳ รอตั้งค่า DNS

### หลังตั้งค่า SSL เสร็จ:
- https://pamoontoy.site
- https://www.pamoontoy.site
- http://pamoontoy.site (จะ redirect ไป HTTPS อัตโนมัติ)

---

## 🔄 การอัปเดตเว็บไซต์

เมื่อต้องการอัปเดตเว็บไซต์:

```bash
cd /var/pamoontoyv2
git pull origin main
npm install
npm run build
sudo systemctl reload nginx
```

---

## 🛠️ คำสั่งที่มีประโยชน์

### ตรวจสอบสถานะ Nginx
```bash
sudo systemctl status nginx
```

### รีสตาร์ท Nginx
```bash
sudo systemctl restart nginx
```

### ดู Nginx logs
```bash
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log
```

### ต่ออายุ SSL certificate (ทำอัตโนมัติทุก 12 ชั่วโมง)
```bash
sudo certbot renew
```

---

## 📦 โครงสร้างโปรเจค

```
/var/pamoontoyv2/
├── dist/                    # ไฟล์ production (Nginx serve จากที่นี่)
├── src/                     # ไฟล์ source code
├── public/                  # ไฟล์ public static
├── package.json            # dependencies
├── vite.config.js          # Vite configuration
├── setup-ssl.sh            # สคริปต์ติดตั้ง SSL
└── DEPLOYMENT-README-TH.md # ไฟล์นี้
```

---

## 🎉 คุณสมบัติของเว็บไซต์

- ✅ ระบบร้านค้าออนไลน์
- ✅ หน้าแอดมิน
- ✅ การจัดการสินค้า
- ✅ การจัดการหมวดหมู่
- ✅ ระบบสมาชิก
- ✅ ตะกร้าสินค้า
- ✅ ระบบสั่งซื้อ
- ✅ Responsive Design
- ✅ Modern UI/UX

---

## 📞 การแก้ไขปัญหา

### เว็บไซต์ไม่แสดง
1. ตรวจสอบ Nginx: `sudo systemctl status nginx`
2. ตรวจสอบ logs: `sudo tail -f /var/log/nginx/error.log`

### DNS ยังไม่ทำงาน
1. รอ 15-30 นาที แล้วลองใหม่
2. Clear DNS cache ในเครื่องของคุณ
3. ใช้ `nslookup` หรือ `dig` ตรวจสอบ

### SSL ติดตั้งไม่ได้
1. ตรวจสอบว่า DNS ตั้งค่าถูกต้อง
2. ตรวจสอบว่าพอร์ต 80 และ 443 เปิดอยู่
3. ลองรันคำสั่งนี้: `sudo certbot --nginx -d pamoontoy.site -d www.pamoontoy.site`

---

## 🔒 ความปลอดภัย

- ✅ SSL/TLS Certificate (HTTPS)
- ✅ Security Headers
- ✅ Gzip Compression
- ✅ Static Asset Caching
- ✅ Auto SSL Renewal

---

**สร้างเมื่อ:** 21 ตุลาคม 2025  
**เซิร์ฟเวอร์:** Ubuntu 24.04 LTS  
**Node.js:** v20.19.5  
**Nginx:** v1.24.0




